// SEOUpdater is no longer needed.
// All SEO logic (canonical, OG tags, title, description) is now handled
// by the useSEO() hook inside each page component.
// This file is kept as an empty component for backward compatibility.
export default function SEOUpdater() {
    return null;
}
